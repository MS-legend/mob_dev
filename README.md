## 🚀 Simple Counter App (React Native/Expo)

Мобильное приложение "Simple Counter App" создано на базе **React Native (Expo)** для демонстрации ключевых аспектов разработки кроссплатформенных приложений. Проект был разработан в рамках лабораторных работ по отладке и тестированию.

---

### ✨ Ключевые возможности

* **Управление состоянием:** Счетчик с функцией сохранения и загрузки значения через асинхронное хранилище (`AsyncStorage`).
* **Сетевые запросы:** Демонстрация GET- и POST-запросов к внешнему API (через кастомный хук `useApi`).
* **Мультимедиа:** Работа с нативными API: камера, галерея (`ImagePicker`), запись и воспроизведение аудио (`expo-av`).
* **Платформенные функции:** Использование `expo-haptics` (тактильная отдача) и `expo-notifications`.
* **Навигация:** Структурированная навигация между экранами (`@react-navigation`).
* **Надежность:** Реализована логика обработки разрешений, очистки ресурсов и сброса данных.

---

### 🛠️ Технологии

* **Фреймворк:** React Native / Expo
* **Навигация:** `@react-navigation/stack`, `@react-navigation/native`
* **Хранение данных:** `@react-native-async-storage/async-storage`
* **Мультимедиа & Устройство:** `expo-av`, `expo-image-picker`, `expo-haptics`, `expo-device`
* **Тестирование:** Jest (для модульного/интеграционного тестирования логики)

---

### 📂 Структура проекта

Логика приложения разделена на три основных каталога: simple-counter-app/ ├── src/ │ ├── components/ # Переиспользуемые элементы (Кнопки, Счетчик) │ ├── hooks/ # Кастомные хуки (useStorage, useApi) │ ├── screens/ # Экраны приложения (Home, Media, Settings и т.д.) │ ├── services/ # Сервисы для внешних данных (apiService.js) │ └── utils/ # Кроссплатформенные утилиты └── App.js # Главный компонент и настройка навигации


---

### ⚙️ Начало работы

#### Требования

* Node.js (рекомендуется v18+)
* npm или Yarn
* Установленное приложение [**Expo Go**](https://expo.dev/client) на мобильном устройстве.

#### Установка и запуск

1.  **Клонирование:**
    ```bash
    git clone [https://www.nic.ru/info/blog/repository/](https://www.nic.ru/info/blog/repository/)
    cd simple-counter-app
    ```

2.  **Установка зависимостей:**
    ```bash
    npm install
    # или
    yarn install
    ```

3.  **Запуск проекта:**
    ```bash
    npx expo start
    # или
    npm start
    ```
    Отсканируйте QR-код, появившийся в терминале, с помощью приложения Expo Go.

---

### 🧪 Результаты тестирования (Лабораторная работа №8)

После проведения отладки (ЛР №7) приложение было протестировано. Все критические модули и пользовательские сценарии прошли проверку.

| Сценарий | Тестируемый функционал | Статус |
| :--- | :--- | :--- |
| **Модульное (useStorage)** | Корректное сохранение, загрузка и сброс данных в AsyncStorage. | ✅ PASSED |
| **Интеграционное (useApi)** | Надежная обработка HTTP-ответов (включая ошибки 4xx/5xx) и повторный запрос (`refetch`). | ✅ PASSED |
| **Функциональное (MediaScreen)** | Запрос и получение разрешений для камеры/галереи/микрофона. | ✅ PASSED |
| **Функциональное (SettingsScreen)** | Функция "Сброс данных" корректно очищает хранилище. | ✅ PASSED |

---

### 📜 Лицензия

Проект распространяется под лицензией **MIT**.